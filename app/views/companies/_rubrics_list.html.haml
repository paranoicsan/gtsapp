%table#rubrics.sub-index.list_company
  %tr
    %th Наименование
    %th Социальная?
    %th
  -if @company.rubrics.count > 0
    -@company.rubrics.each do |rub|
      %tr
        %td= rub.name
        %td= check_box_tag("social", 1, rub.social?, {:enabled => false})
        %td
  -else
    %tr
      %td{:colspan => 3}
        Рубрик не назначено.