%table#rubrics.sub-index.list_company
  %tr
    %th Наименование
    %th Социальная?
    %th
  -if @company.rubrics.count > 0
    -@company.rubrics.each do |rub|
      %tr
        %td= rub.name
        %td= check_box_tag("social", 1, rub.social?, { :disabled => "disabled" })
        %td= link_to "Удалить", company_delete_rubric_path(@company, rub), :confirm => 'Are you sure?', :remote => true
  -else
    %tr
      %td{:colspan => 3}
        Рубрик не назначено.