#message
  - if flash[:notice]
    #notice.alert.alert-success
      = flash[:notice]
  - if flash[:error]
    #warning.alert.alert-error
      Ошибка:
      - if flash[:error].is_a?(Array)
        - flash[:error].each do |msg|
          = msg
      - else
        = flash[:error]
