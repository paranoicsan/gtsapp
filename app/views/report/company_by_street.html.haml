# encoding: utf-8
= render :partial => "shared/messages"

.page-header
  %h1
    Отчёт компаний по улицам
    %p
      %small
        = link_to "Отчёты", reports_path

.container-fluid
  .row-fluid
    .span10
      .well
        = form_tag report_prepare_company_by_street_path, :remote => true, :method => :post, :class => 'form-horizontal well' do
          .control-group
            = label_tag 'Город', nil, :class => 'control-label'
            .controls
              = autocomplete_field_tag 'address_city', nil, addresses_autocomplete_city_name_path,
              :placeholder => "Начните печатать для выбора из списка", :id_element => '#city_id', :id => 'address_city_id',
              :onKeyUp => 'onCityChanged()', :onChange => 'onCityChanged()'
              -#-city_id = @address.city ? @address.city.id : ''
              -#= hidden_field_tag 'city_id', city_id
          -#.control-group
          -#  = f.label :street, 'Улица', :class => 'control-label'
          -#  .controls
          -#    -s = @address.street ? @address.street.name : ''
          -#    = autocomplete_field_tag 'address_street', s, '', :placeholder => "Начните печатать для выбора",
          -#    :id => 'address_street', :id_element => '#street_id'
          -#    -street_id = @address.street ? @address.street.id : ''
          -#    -street_city_id = @address.street ? @address.street.city_id : ''
          -#    = hidden_field_tag :street_id, street_id
          -#    = hidden_field_tag :street_city_id, street_city_id
          .form-actions
            = submit_tag "Показать", {:disabled => "disabled", :id => "do_report_company_by_street", :class => 'btn'}
  -#
  -##report_results_container
  -#  =render :partial => "report_by_agent_result"